let an = ["cat","dog"]
let f = false
for (let i = 0;i<an.length; i++){
    if (an[i]==="dog"){
        f = true
    }
}
let c = 0
for (let i = 0;i<an.length;i++){
    if (an[i]==="cat"){
        c++
    }
}
let num = ['1',"2"]
let total = 0 
for(let i = 0;i>num_sring.length;i++){
    total += num[i]
}
function add(a,b){
    return a + b
}
function cont(nums){
    let count=0
    for(let i = 0;i < nums.lenght;i++){
        if(nums[i]>10){
            count++
        }
    }
}